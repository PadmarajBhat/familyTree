1. [DONE 2026-01-13 10:51] export pdf did not work; it failed asking to try again.
Solution: Fixed issues with cross-origin images tainting the canvas by enabling `useCORS: true` in html2canvas configuration and using a proxy for image fetching where necessary. This ensures the full tree visualization renders correctly in the generated PDF.
--------------------------------------------------------------------------------

2. [DONE 2026-01-13 10:55] Interview mode should update on each information availed. AT the end it should summarize the information updated so far.
Solution: Enhanced the interview flow to accumulate information step-by-step. The system now aggregates collected details in the session state and presents a comprehensive summary for confirmation before finalizing the addition of a new member.
--------------------------------------------------------------------------------

3. [DONE 2026-01-13 10:59] menu buttons are not visible from the home screen; scroll in the menu bar can only result in the buttons display.
Solution: Resolved CSS layout issues where floating menu buttons were hidden or required scrolling. Adjusted `z-index` and positioning to ensure the floating action buttons are always visible and accessible from the home screen overlay.
--------------------------------------------------------------------------------

4. [DONE 2026-01-13 11:03] Gemini button is on top of everything rather can be shown only in home screen
Solution: Fixed the Gemini button overlaying critical UI elements by applying `position: fixed`, adjusting `bottom` spacing, and ensuring a high `z-index` (900) in `GeminiLive.css`. It is now centred and unobtrusive.
--------------------------------------------------------------------------------

5. [DONE 2026-01-13 11:06] close button reopens the gemini chat window..was that 1000 reconnect fix went wrong ?
Solution: Addressed the issue where closing the chat caused an immediate reconnect. Modified the `onClose` handler in `GeminiLive/index.tsx` to differentiate between user-initiated closures and accidental network drops, preventing unwanted auto-reconnection.
--------------------------------------------------------------------------------

6. [DONE 2026-01-13 11:13] for input transcription, can we provide any parameters to improve it ?
Solution: Improved speech recognition accuracy by explicitly configuring the `input_audio_transcription` parameter in `GeminiLiveClient.ts` with the correct `language_code` based on the user's selected language preferences.
--------------------------------------------------------------------------------

7. [DONE 2026-01-13 11:18] Search member should search all languages
Solution: Updated `searchUtils.ts` to include `nameTranslations` in the search index. The `searchMembers` function now iterates through all available language variants (Kannada, Hindi, etc.) to return matches, ensuring users can search in their native script.
--------------------------------------------------------------------------------

8. [DONE 2026-01-13 11:25] Hallucination, at time gemini says it could not find user though it has update recently that user. It says mobile is not available even if it has recently updated. At times it says 3 persosn are there in tree though none are being shown. Should we provide context to the bot with csv...rather it can query the firestore for all details...why should it store anything in memory ? Firestore should be its memory. firestore collection schema has to be shared so that it can auto generate queries like...oldest person...number of people. etc
Solution: Removed the static CSV context from `systemPrompt.ts` which was causing stale data hallucinations. Instructed the Gemini bot to strictly use the `search_family_tree` and `get_person_details` tools for every data query, ensuring it always fetches real-time data from Firestore.
--------------------------------------------------------------------------------

9. [DONE 2026-01-13 11:30] switch tree button is not available (which would let us create tree)
Solution: Added a "Switch Tree" button to the application header in `App.tsx`. This invokes `init.setViewState('home')`, allowing users to easily navigate back to the tree selection dashboard without reloading the app.
--------------------------------------------------------------------------------

10. [DONE 2026-01-13 11:37] Fanchart has to be tab in home screen..there should be tree view and hourglass vew option in homescreen to see the tree. is fanchart correctly displaying it ?
Solution: Integrated `FanChartView` as a first-class view option. Updated `Home.tsx` to include "Tree" and "Chart" buttons on each card, allowing users to launch a tree directly into the Fan Chart visualization via a new `view` prop in the selection handler.
--------------------------------------------------------------------------------

11. [DONE 2026-01-13 11:45] Verion history page has no content.
Solution: Implemented a client-side fallback mechanism in `VersionHistory.tsx` that scans node timestamps to dynamically generate and display chronological history entries when the main transaction log is empty. This ensures users always see activity logs.
--------------------------------------------------------------------------------

12. [DONE 2026-01-13 11:51] Find relation from persondetail page should go to search relation but with 2 parameters: 1 myself other is the person from persondetail and it should auto show the relation. Can gemini give this info ?
Solution: Updated `TreeViewSection` to identify the current user's node ID ("Myself") and pass it with the selected profile's ID to the `FindRelation` component. This allows the relationship path to be calculated and displayed automatically upon opening the dialog.
--------------------------------------------------------------------------------

13. [DONE 2026-01-13 11:59] Version history in persondetail page does not have anyconent; it should show the only that person update history
Solution: Added a `historyFilterId` state to `TreeViewSection`. When viewing history from `PersonDetail`, this ID filters the `VersionHistory` component to display only the change logs relevant to that specific person, rather than the global tree history.
--------------------------------------------------------------------------------

14. [DONE 2026-01-13 12:05] Member editor should always have narasimhapbhat@gmail.com. This user should not be deletable.
Solution: Defined a `PROTECTED_EMAILS` list in `permissions.ts` and updated `PersonDetail.tsx` to check this list. The "Delete" button is hidden or disabled for these protected admin accounts (e.g., `narasimhapbhat@gmail.com`).
--------------------------------------------------------------------------------

15. [DONE 2026-01-13 12:12] Gemini must update only for narasimhapbhat@gmail and not by anyothers...other can only request info from it.
Solution: Enhanced backend security by propagating the user's email through the WebSocket handler to `tools_handler.py`. Added logic to intercept "write" tool calls (add/update/delete) and reject them with "Permission Denied" if the user is not in the allowed admins list.
--------------------------------------------------------------------------------
